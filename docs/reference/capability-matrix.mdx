---
title: Capability matrix
description: The full per-provider RepoCapabilities reference.
sidebar:
  label: Capability matrix
---

A complete reference of the `RepoCapabilities` values for each provider — tag dates, repo search, owned
filter, webhook events, webhook verification, and archive formats.

Every provider exposes this object as `capabilities`, also surfaced on `client.capabilities`. The client
gates on these values and throws `unsupported` rather than silently dropping an option — see
[Capabilities](/docs/concepts/capabilities).

## Matrix

| Capability            | Type                            | GitHub                    | GitLab                    | Bitbucket        | Azure DevOps  | Gitea                     |
| --------------------- | ------------------------------- | ------------------------- | ------------------------- | ---------------- | ------------- | ------------------------- |
| `tagDates`            | `boolean`                       | `false`                   | `true`                    | `true`           | `false`       | `false`                   |
| `repoSearch`          | `boolean`                       | `true`                    | `true`                    | `true`           | `false`       | `true`                    |
| `ownedRepoFilter`     | `boolean`                       | `true`                    | `true`                    | `true`           | `false`       | `true`                    |
| `commitUserRef`  | `boolean`                       | `true`                    | `false`                   | `true`           | `false`       | `true`                    |
| `webhookEvents`       | `WebhookEventType[]`            | `push, tag_push, release` | `push, tag_push, release` | `push, tag_push` | `push, tag_push` | `push, tag_push, release` |
| `webhookVerification` | `WebhookVerificationMethod`     | `hmac-sha256`             | `shared-token`            | `hmac-sha256`    | `basic-auth`  | `hmac-sha256`             |
| `archiveFormats`      | `ArchiveFormat[]`               | `zip, tar.gz`             | `zip, tar.gz`             | `zip, tar.gz`    | `zip`         | `zip, tar.gz`             |

## What each field gates

<TypeTable
  type={{
    tagDates: { type: 'boolean', description: 'Whether listed tags carry a date. When false, Tag.date is undefined (GitHub, Azure DevOps, Gitea).' },
    repoSearch: { type: 'boolean', description: 'Whether repos.list accepts a free-text query. Passing query without it throws unsupported.' },
    ownedRepoFilter: { type: 'boolean', description: 'Whether repos.list accepts owned: true. Passing it without support throws unsupported.' },
    commitUserRef: { type: 'boolean', description: 'Whether commit actors can carry a resolved account (GitActor.user). When false, user is always undefined (GitLab, Azure DevOps).' },
    webhookEvents: { type: 'WebhookEventType[]', description: 'The events webhooks.create / update accept. Any other event throws unsupported.' },
    webhookVerification: { type: "'hmac-sha256' | 'shared-token' | 'basic-auth'", description: 'How verifyWebhook checks an incoming delivery for this provider.' },
    archiveFormats: { type: 'ArchiveFormat[]', description: 'The formats downloadArchive can return. Any other format throws unsupported.' },
  }}
/>

## Reading capabilities at runtime

Because capabilities are plain data, you can branch on them before offering a feature:

```ts
if (client.capabilities.repoSearch) {
  const { data } = await client.repos.list({ query: userInput });
}

if (client.capabilities.webhookEvents.includes('release')) {
  // offer release webhooks in the UI
}
```

<Card title="Provider support" icon="table" href="/docs/reference/provider-support">
  The broader feature-by-provider matrix, including auth and self-hosting.
</Card>
